import { BookOpen, Upload } from "lucide-react";

export function IconUpload() {
  return <Upload size={20} strokeWidth={1.75} aria-hidden="true" />;
}

export function IconSamples() {
  return <BookOpen size={20} strokeWidth={1.75} aria-hidden="true" />;
}
